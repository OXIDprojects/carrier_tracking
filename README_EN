Installation
------------

== copy files ==
copy folders&files from "copy_this" 1:1 to shoproot


== execute sql ==
execute the the queries from "setup+doku/install.sql" or "setup+doku/update.sql". After this regenerate the views


== modifie templates ==
compare & merge the templates from "changed_full" with templates from admin &shop


== activate module ==
activive this modul under [Extensions] --> [Modules] --> "Parcel Service" --> [Tab Overview]
